// SPDX-License-Identifier: MIT
pragma solidity ^0.8.34;

// `IVerifier` is declared in the bb-generated `Verifier.sol` (the file CI regenerates); this
// re-export lets consumers import it from the interfaces directory like every other seam.
import {IVerifier} from "../Verifier.sol";
